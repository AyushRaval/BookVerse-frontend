# API Contract

> Empty skeleton. Fill this in as Phase 1 backend endpoints are built — every endpoint
> should be documented here with request/response shape before (or as) it's implemented,
> so any AI tool can build against a known contract instead of guessing.

**Status:** Not yet started — no endpoints exist yet (Phase 0).

## Format for each endpoint, once added:

```
### POST /api/v1/<path>
**Purpose:** one line
**Auth:** required / not required
**Request body:**
```json
{ }
```
**Response (200):**
```json
{ }
```
**Errors:** notable error cases and status codes
```

---

## Planned for Phase 1 (not yet implemented)

- `POST /api/v1/videos/upload` — accept a squat video upload
- `GET /api/v1/videos/{id}/analysis` — retrieve form-analysis results for a processed video

Exact shapes to be defined when Phase 1 implementation begins.
